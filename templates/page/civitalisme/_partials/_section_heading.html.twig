<div class="section-heading section-heading--stack">
    <div>
        {% if eyebrow is defined and eyebrow %}
            <p class="eyebrow">{{ eyebrow }}</p>
        {% endif %}
        <h2>{{ title }}</h2>
    </div>

    {% if lead is defined and lead %}
        <p class="section-intro">{{ lead }}</p>
    {% endif %}
</div>
